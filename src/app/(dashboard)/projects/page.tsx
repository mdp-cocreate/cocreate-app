import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Projets',
  description: 'Tous les projets postés par la communauté Cocreate',
};

export default function Projects() {
  return <h1>Projects</h1>;
}
